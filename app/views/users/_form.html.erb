<%= form_with(model: [user], local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class = "field">
    <%= form.label :email %><br>
    <%= form.text_field :elail %>
  </div>

  <div class = "field">
    <%= form.label :usermane %><br>
    <%= form.text_area :username %><br>
  </div>

  <div class = "field">
    <%= form.hidden_field :name %><br>
    <%= form.file_field :name %>
  </div>

  <div class = "actions">
    <%= form.submit %>
  </div>
<% end %>